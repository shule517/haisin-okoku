export default class Packet {
    addStr(str) {
        //binary += str;
    }
    addInt(num) {
        //binary += num;
    }
    readStr() {
        return 'str';
    }
    readInt() {
        return 10;
    }
}
